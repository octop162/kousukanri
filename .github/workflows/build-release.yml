name: Build and Release Executable

on:
  push:
    tags:
      - 'v*'

jobs:
  build-release:
    name: Build and Release for Windows
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync

      - name: Build GUI
        run: |
          uv run python -m nuitka --standalone --enable-plugin=pyside6 --windows-console-mode=disable --windows-icon-from-ico=icon.ico --include-data-dir=templates/=templates/ --output-dir=dist --output-filename=kousu-kanri-gui.exe --assume-yes-for-downloads main.py

      - name: Prepare release assets
        run: |
          New-Item -ItemType Directory -Path dist/main.dist/data -Force
          Compress-Archive -Path dist/main.dist/* -DestinationPath kousukanri-windows.zip
        shell: pwsh

      - name: Create Release and Upload Asset
        uses: softprops/action-gh-release@v2
        with:
          files: kousukanri-windows.zip
          overwrite: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
